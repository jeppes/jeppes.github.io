{
  "name": "jeppes.github.io",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "browser-sync start --server --files '**/*.html,**/*.css' --ignore 'node_modules,public,release'",
    "format": "prettier --write '**/*.html' '*.css'"
  },
  "devDependencies": {
    "browser-sync": "^3.0.4",
    "prettier": "^3.9.6"
  }
}
